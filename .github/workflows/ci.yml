name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run unit tests
        run: bun test src/ tests/

      - name: Red-team the destructive gate (robustness — fails on a catchable gap or false positive)
        run: bun run src/cli/index.ts redteam --rules rules/destructive-rules.json

      - name: CI pipeline audit — every publish step declared (AF-T-710, INF-AFW-007)
        run: bun run src/cli/index.ts ci-audit --dir .

      - name: Run agent governance scenarios (AEGIS on AEGIS)
        run: bun src/test-agents/runner.ts
        env:
          HOME: /tmp/aegis-ci-home

      - name: Build CLI
        run: bun build src/cli/index.ts --target=bun --outdir=dist/
